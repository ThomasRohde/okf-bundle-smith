interface:
  display_name: OKF Data Catalog
  short_description: Turn datasets, tables, and metrics into a cross-linked OKF catalog.
  brand_color: "#2F6F5E"
  default_prompt: Turn this data catalog into an OKF bundle with join paths and metrics.
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: mcp
      value: okf-tools
      description: OKF Bundle Smith MCP server
